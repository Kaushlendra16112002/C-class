#include <stdio.h>
void main()
{
    int arr[100], i, j, n;
    printf("Enter the no of Elements:\n");
    for (i = 0; i < n; i++)
    {
        scanf("%d", &arr[n]);
    }

    for (i = 0; i < n; i++)
    {
        for(j=0; j<)
    }
}
